{
  "name": "bechdou-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bechdou marketplace backend — Express + SQLite (node:sqlite) REST API",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --experimental-sqlite --env-file-if-exists=.env index.js",
    "dev": "node --experimental-sqlite --env-file-if-exists=.env --watch index.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "stripe": "^16.0.0"
  }
}
